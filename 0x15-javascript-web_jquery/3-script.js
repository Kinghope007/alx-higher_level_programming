$(document).ready(function() {
    // wait for the DOm ro be fully loaded
    $('#red_header').click(function() {
        // when the header is clicked
        $('header').addClass('red');
    });
});